language: node_js
node_js:
  - 7.10.0
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8

install:
  - cd client
  - npm install
  - cd ../server
  - npm install
  - cd ../electron
  - npm install