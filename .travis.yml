os: osx
osx_image: xcode8.3
language: node_js
node_js:
  - 7.10.0

before_install:
  - brew update
#  - brew install gnu-tar graphicsmagick xz
#  - brew install wine --without-x11
#  - brew install mono

install:
  - cd client
  - npm install --silent
  - npm run build
  - cd ../server
  - npm install --silent
  - npm run build
  - cd ../electron
  - npm install --silent
  - npm run build:mac
#  - npm run build:windows