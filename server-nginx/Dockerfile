FROM nginx

# Copy custom configuration file from the current directory
COPY ./nginx.conf /etc/nginx/nginx.conf
COPY mime.types /etc/nginx/mime.types

# RUN mkdir -p /usr/src/app/images
# RUN mkdir -p /usr/src/app/client/www

# RUN nginx -t